// Email configuration
// This file is kept for backward compatibility
// The actual email service is in backend/utils/emailService.js

module.exports = {
  // Email configuration is now in .env file
  // EMAIL_USER and EMAIL_APP_PASSWORD
};
